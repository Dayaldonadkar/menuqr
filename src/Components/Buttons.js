import React from "react";
import { Link } from "react-router-dom";

const Buttons = () => {
  return (
    <div>
      <button className="uppercase">Starters</button>
    </div>
  );
};

export default Buttons;
